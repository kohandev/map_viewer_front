import { Box, styled } from "@mui/material";

export const LayoutBox = styled(Box)(() => ({
  width: "100vw",
  height: "100vh",
  display: "flex",
  flexDirection: "column",
  backgroundColor: "#b8f8f8",
}));
